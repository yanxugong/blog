# This workflow will run tests using node and then publish to github pages

name: gh-pages publisher  🚀

on:
  push:
    branches: [main]

jobs:
  publish-gh-pages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 12
      # - run: npm ci
      - run: git config user.name "Yanxu Gong" && git config user.email "745710524@qq.com"
      - run: npm run deploy
        env:
          GH_TOKEN: ${{secrets.SECRET_GITHUB_TOKEN}}
